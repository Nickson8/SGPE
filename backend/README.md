# SGPE Backend

API FastAPI (somente leitura) do SGPE. Os modelos SQLAlchemy espelham o
esquema criado pelos scripts em `../init-scripts/`; o backend nunca cria,
altera ou migra o banco — apenas consulta.

Veja o `README.md` da raiz do projeto para instruções de execução.
